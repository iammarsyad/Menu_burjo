#include <iostream>
#include <string>
#include <vector>

using namespace std;

int wuhuy()
{
	vector<int,string> a;
	a.assign(10,100);
	a.push_back(1);
	cout << a.size() << endl;

	return 0;
}